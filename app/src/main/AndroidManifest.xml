<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.qiudaoyu.test">

    <application
        android:name="com.qiudaoyu.test.TabletApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="com.qiudaoyu.test.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name="com.qiudaoyu.test.SecondActivity">

        </activity>
        <activity android:name="com.qiudaoyu.test.ThirdActivity">

        </activity>

        <service
            android:name="com.qiudaoyu.service.RemoteService"
            android:exported="true"
            android:process=":test">

        </service>
    </application>

</manifest>